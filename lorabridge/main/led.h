#pragma once
// ============================================================
// Событийный светодиод (V2.9). На платах без LED — no-op.
// ============================================================
#include <stdint.h>
#include <stdbool.h>

void led_init(void);
void led_set(bool on);        // держать (PTT-сессия и т.п.)
void led_pulse(uint32_t ms);  // вспыхнуть и погаснуть (событие)
